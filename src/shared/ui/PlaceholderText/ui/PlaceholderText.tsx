import "./PlaceholderText.scss";

const PlaceholderText = () => {
	return (
		<h2 className="placeholder-text h3">
			The project only includes a home page and a contact page
		</h2>
	);
};

export default PlaceholderText;
